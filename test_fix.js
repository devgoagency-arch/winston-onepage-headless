const url = 'https://tienda.winstonandharrystore.com/categoria/accesorios-hombre/'; let clean = url; ['https://tienda.winstonandharrystore.com', 'https://winstonandharrystore.com'].forEach(d => { if (d) clean = clean.replace(d, ''); }); if (!clean || clean === '' || clean === '/') clean = '/'; if (!clean.startsWith('/')) clean = '/' + clean; clean = clean.replace(/\/$/, ''); console.log('Fixed URL:', clean);
