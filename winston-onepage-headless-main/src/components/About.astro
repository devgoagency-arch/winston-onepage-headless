---

---

<section id="nosotros" class="about-lv-hero">
    <div class="video-container">
        <!-- Overlay sutil para legibilidad -->
        <div class="video-overlay"></div>

        <div class="iframe-wrapper">
            <iframe
                src="https://www.youtube.com/embed/XGnN4DTsdD8?autoplay=1&mute=1&loop=1&playlist=XGnN4DTsdD8&controls=0&showinfo=0&rel=0&iv_load_policy=3&modestbranding=1&disablekb=1"
                frameborder="0"
                allow="autoplay; encrypted-media"
                allowfullscreen
            >
            </iframe>
        </div>
    </div>

    <div class="container about-text-overlay">
        <div class="about-content-corner">
            <h2 class="about-title">
                Winston & Harry, <br />
                Maestros de la Tradición
            </h2>

            <p class="about-description">
                Winston & Harry nace de la pasión por el detalle y el respeto
                por las técnicas tradicionales de zapatería. Cada par es una
                obra única, confeccionada por manos colombianas que transforman
                los cueros más finos en piezas de arte para tus pies.
            </p>

            <!-- <a href="#tienda" class="discover-link">Descubrir colección</a> -->
        </div>
    </div>
</section>

<style>
    .about-lv-hero {
        position: relative;
        height: 100vh;
        width: 100%;
        overflow: hidden;
        background-color: #000;
        display: flex;
        align-items: flex-end; /* Alineamos el contenido abajo */
        padding-bottom: 8vh; /* Espacio desde el borde inferior */
        z-index: 10; /* Mayor que el banner anterior para el efecto parallax */
        margin-top: -100vh; /* Truco para que esta sección suba y cubra el banner fijo */
    }

    .video-container {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: 0;
    }

    .video-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        /* Gradiente para proteger el texto en la esquina inferior izquierda */
        background: radial-gradient(
            circle at 10% 90%,
            rgba(0, 0, 0, 0.45) 0%,
            rgba(0, 0, 0, 0.1) 50%,
            transparent 100%
        );
        z-index: 2;
    }

    .iframe-wrapper {
        position: relative;
        width: 100%;
        height: 100%;
        pointer-events: none;
    }

    .iframe-wrapper iframe {
        position: absolute;
        top: 50%;
        left: 50%;
        width: 100vw;
        height: 56.25vw;
        min-height: 100vh;
        min-width: 177.77vh;
        transform: translate(-50%, -50%);
        opacity: 0.9;
    }

    .about-text-overlay {
        position: relative;
        z-index: 3;
        width: 100%;
        padding: 0 4rem;
    }

    .about-content-corner {
        max-width: 420px; /* Tamaño similar al del ejemplo de Louis Vuitton */
        color: #fff;
    }

    .about-title {
        font-family: var(--font-titles);
        font-size: clamp(1.5rem, 3vw, 2.2rem);
        font-weight: 700;
        line-height: 1.1;
        margin-bottom: 1.5rem;
        text-transform: uppercase;
        letter-spacing: 2px;
        color: #fff;
    }

    .about-description {
        font-family: var(--font-paragraphs);
        font-size: 0.85rem;
        line-height: 1.6;
        font-weight: 300;
        margin-bottom: 2rem;
        color: rgba(255, 255, 255, 0.85);
        letter-spacing: 0.01em;
    }

    .discover-link {
        font-family: var(--font-paragraphs);
        font-size: 0.8rem;
        font-weight: 400;
        color: #fff;
        text-decoration: none;
        border-bottom: 1px solid rgba(255, 255, 255, 0.5);
        padding-bottom: 4px;
        transition: border-color 0.3s ease;
        text-transform: capitalize;
    }

    .discover-link:hover {
        border-bottom-color: #fff;
    }

    @media (max-width: 768px) {
        .about-lv-hero {
            padding-bottom: 4rem;
        }
        .about-text-overlay {
            padding: 0 2rem;
        }
        .about-title {
            font-size: 1.8rem;
        }
    }
</style>
